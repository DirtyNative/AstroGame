using AstroGame.Shared.Models.Stellar.StellarSystems;
using UnityEngine;

public class GalaxyInstantiator : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Galaxy galaxy = new Galaxy();
    }

    // Update is called once per frame
    void Update()
    {
    }
}